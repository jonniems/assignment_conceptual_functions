//declaration
const makeSandwich = function (topping) {
    console.log('Get a slice of bread.');
    console.log('Add some ' + topping + ' on top of it.');
    console.log('Add a  slice of bread on top of the ' + topping + '.');
    console.log('There you go, a sandwich with... ' + topping)
}
//call
makeSandwich('hagelslag');
makeSandwicht('pindakaas');